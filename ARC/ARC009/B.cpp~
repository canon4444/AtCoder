#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main()
{
    int b[10];
    for( int i = 0; i < 10; ++i )
        cin >> b[i];

    int N;
    cin >> N;

    vector<string> a;
    string str;
    for( int i = 0; i < N; ++i ){
        cin >> str;
        a.push_back(str);
    }


    int leni, lenj;
    for( int i = 0; i < N; ++i ){
        leni = a[i].length();
        for( int j = 0; j < N; ++j ){
            lenj = a[j].length();
            if( lenj < leni ){
                str = a[i];
                a[i] = a[j];
                a[j] = str;
            } else if( lenj == leni ){
